# from Agents import *
# from Agents.AuctionedObject import Auctioneer, AuctionedObject
# from Agents.AuctionHouse import AuctionHouse

# n_bidders = 10
# bidders = np.array([bd.SimpleBidder() for _ in range(n_bidders)])
#
# n_objects = 10
# auctioned_objects = np.array([AuctionedObject(0, 1) for i in range(n_objects)])
# auctioneer = Auctioneer(auctioned_objects)

# n_rounds = 10
# house = AuctionHouse(n_rounds, auctioneer, bidders)

# Press the green button in the gutter to run the script.
# if __name__ == '__main__':
    # house.run()
    # df = house.bidder_tracker.make_dataframe(variables=['bids'])
    # df.mean(axis=1).plot()
    # plt.show()

# See PyCharm help at https://www.jetbrains.com/help/pycharm/
